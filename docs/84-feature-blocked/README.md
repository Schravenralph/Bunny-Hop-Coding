# Features Blocked

This directory contains features that are blocked and cannot proceed.

## How to Use

- Move features here from `81-feature-in-progress/` when blocked
- Document blocking issue and dependencies
- Move back to `81-feature-in-progress/` when unblocked
- Move to `85-feature-cancelled/` if permanently blocked

## Blocked Features

_No blocked features yet_

